<div class="container">
  <h1><%= @user.name %>のページ</h1>
  <p>email:<%= @user.email %></p>
  <p>
    <strong>Image:</strong>
    <%= image_tag(@user.image.url,:size => "100x100") %>
  </p>

<!-- <% if @favorites_blogs.present? %>
  <% @favorites_blogs.each do |blog| %>
    <div class="col-xs-12 col-md-6 blog-panel">
      <h3><%= blog.user.name %> </h3>
      <h2><%= blog.title %></h2>
      <p><%= blog.content %></p>
    </div>
  <% end %>
<% end %> -->
</div>
<%= link_to '編集', edit_user_path,class: 'btn btn-primary'%>
<%= link_to "ブログ一覧に戻る",blogs_path,class: 'btn btn-info' %>
<%= link_to "お気に入り",user_favorites_path(current_user.id),class: 'btn btn-danger' %>
