<h1>View</h1>
<div class="container">
  <div id="new-post-link">
     <%= link_to 'newpost',new_blog_path,class: 'btn btn-primary' %>
  </div>
<div class='row'>
<% @blogs.each do |blog| %>
<div class="col-xs-12 col-md-6 blog-panel">
  <h3><%= blog.title %></h3>
  <h4><%= blog.user.name %></h4>
  <div class="blog-links">
    <% if current_user.present? %>
      <%= link_to "show",blog_path(blog.id),class: 'btn btn-default' %>
      <% if current_user.id  ==  blog.user_id %>
        <%= link_to "edit", edit_blog_path(blog.id), class: 'btn btn-info'%>
        <%= link_to"destroy",blog_path(blog.id),method: :delete ,data: {confirm: '本当に？'},class: 'btn btn-danger' %>
      <% end %>
    <% end %>
  </div>
</div>
<% end %>
</div>
</div>
